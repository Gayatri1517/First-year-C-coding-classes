#include <stdio.h>
int main()
{
    char str[100];
    int i = 0;
        
    printf("\n Please Enter any String  :  ");
    scanf("%s", str);
        
    while (str[i] != '\0')
    {
    	if(str[i]=='a'){
    	str[i]=str[i]-32;
    	printf("%c",str[i]);}
		i++;
		printf("%c",str[i]);
    }
     
    return 0;
}
