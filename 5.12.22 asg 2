n=int(input('enter the n numbers'))
num=int(input('enter the number'))
l=num
s=num
i=1
while i<n:
    num=int(input('enter the numbers'))
    if num>l:
        l=num
    if num<s:
        s=num
    i+=1
print(l-s)
