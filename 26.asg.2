#include<stdio.h>
#include<string.h>
int main()
{
	char str[20];
	int i,count=0,n;
	printf("enter a string\n");
	gets(str);
	n=strlen(str);
	for(i=0;i<n/2;i++)
	{
		if(str[i]==str[n-i-1])
		count++;
	}
	if(count==i)
	printf("Palindrome\n");
	else
	printf("Not Palindrome\n");
	return 0;
}
